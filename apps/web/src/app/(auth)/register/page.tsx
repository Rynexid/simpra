import { RegisterForm } from "@/components/register-form";

export const metadata = {
  title: "Register",
  description: "Create your Simpra account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
