import { LoginForm } from "@/components/login-form";

export const metadata = {
  title: "Login",
  description: "Sign in to your Simpra account",
};

export default function LoginPage() {
  return <LoginForm />;
}
